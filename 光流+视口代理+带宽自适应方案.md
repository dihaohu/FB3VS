# 光流 + 视口代理 + 带宽自适应：全景巡检视频传输方案

---

## 1. 方案概述

### 1.1 核心思想

在机器人移动全景巡检场景中，将三个信号统一纳入 tile 级码率分配决策：

- **光流信号**：回答"哪些区域人眼追不上"（物理感知上限）
- **视口代理**：回答"人现在在看哪里"（实际注视区域）
- **带宽估计**：回答"总共能投多少码率"（全局资源约束）

三者关系：**带宽决定总量，光流 + 视口决定分配。**

### 1.2 系统架构

```
┌──────────────────────────────────────────────────┐
│                   输入信号                         │
├───────────────┬──────────────┬────────────────────┤
│  全景视频帧    │  5G 上行链路  │  机器人运动状态     │
│  (2880×1440)  │  (RTMP/SRT)  │  (速度 v, 方向)    │
└───────┬───────┴──────┬───────┴────────┬───────────┘
        │              │                │
        ▼              ▼                ▼
┌───────────┐  ┌───────────┐  ┌──────────────────┐
│  光流计算  │  │  带宽估计  │  │  视口代理         │
│  (帧级)   │  │  (秒级)   │  │  (帧级, 零数据)   │
└─────┬─────┘  └─────┬─────┘  └────────┬─────────┘
      │              │                │
      ▼              ▼                ▼
┌──────────────────────────────────────────────────┐
│              Tile 优先级融合                       │
│   W_tile = α·w_view + β·(1-w_flow) + γ·w_task   │
└──────────────────────┬───────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│              QP 偏移计算                          │
│   QP_tile(i) = QP_base(B_est) + ΔQP(W_tile(i))  │
└──────────────────────┬───────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│           HEVC Tile-based 编码 (FFmpeg x265)      │
│           45 个 tile (9×5), 差异化 QP             │
└──────────────────────┬───────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│           RTMP/SRT 推流 → 服务器 → 播放端          │
└──────────────────────────────────────────────────┘
```

---

## 2. 模块详细设计

### 2.1 光流计算模块（帧级）

**目标**: 对每一帧全景图像，计算球面上每个 tile 的平均光流幅值。

**输入**: 连续两帧 equirectangular 图像 (2880×1440, YUV420p 或 RGB)

**算法选择**:

| 方法 | 速度 | 精度 | 推荐度 |
|---|---|---|---|
| Farneback (OpenCV) | 快 | 中 | ★★★★★ 首选 |
| DIS (OpenCV) | 非常快 | 中 | ★★★★ 备选 |
| RAFT (PyTorch) | 慢 | 高 | ★★★ 离线分析可 |

**推荐**: `cv2.calcOpticalFlowFarneback()`。在 2880×1440 分辨率上约 15-30ms/帧（GPU 加速后更快）。

**具体步骤**:

```
Step 1: 读取当前帧和前一帧
Step 2: 转灰度图
Step 3: 计算稠密光流: flow = Farneback(prev_gray, curr_gray)
Step 4: 光流幅值: mag = sqrt(flow_x² + flow_y²)
Step 5: 球面权重修正（补偿 equirectangular 投影的面积畸变）
        - 两极区域像素被拉伸 → 光流被高估 → 需乘以 cos(lat)
        - w_sphere(θ) = cos(θ), θ ∈ [-π/2, π/2]
Step 6: 按 tile 划分, 对每个 tile i:
        flow_tile[i] = mean(mag[i] × w_sphere[i])
Step 7: 归一化: w_flow[i] = flow_tile[i] / max(flow_tile)
```

**球面修正说明**:
- Equirectangular 投影在球面两极（图像的顶边和底边）将小面积拉伸成大像素块
- 物理上, 球面上同一角速度在两极和赤道产生的像素位移不同
- 修正后，w_flow 反映的是"球面上的真实角速度"而非"图像上的像素速度"

**输出**: 45 维向量 `w_flow[45]`，值域 [0, 1]，值越大表示该 tile 光流越大（越看不清）

---

### 2.2 视口代理模块（帧级，零数据依赖）

**目标**: 预测管理人员的视口位置，作为 tile 优先级分配的第二个信号。

**为什么需要代理而不是真实视口**:
- 真实视口需要 viewer 端回传视口数据到编码端 → 引入上行延迟
- 即使有回传通道，RTT 延迟（5G 典型 20-50ms）使"当前视口"已是过时信息
- 在巡检场景中，视口受机器人运动强约束 → 可以用运动状态推测

**代理策略（三级，按复杂度递增）**:

#### 策略 1: 光流最小值代理（推荐，零额外成本）

```
直觉: 人眼自然锁定在画面中最稳定的区域 → 光流最小的 tile 就是视口中心

视口中心 tile = argmin_i(flow_tile[i])
视口权重 w_view[i] = Gaussian(distance(i, center_tile), σ)
```

- 优点: 完全复用光流计算结果，零额外开销
- 局限: 无法区分"静态但无关"和"静态且重要"（需配合 w_task）

#### 策略 2: 速度先验代理（需机器人速度信息）

```
直觉: 高速时视口集中在运动前方，低速时分散探索

视口中心方向 = 机器人前进方向（从运动规划/IMU 获得）

低速 (v < 0.5 m/s):  σ_view = 大 (60°-90°) → 视口分散
中速 (0.5 ≤ v < 2 m/s): σ_view = 中 (30°-60°)
高速 (v ≥ 2 m/s):       σ_view = 小 (15°-30°) → 视口集中

w_view[i] = Gaussian(angle_distance(i, forward_direction), σ_view(v))
```

- 优点: 利用了速度对视口分布的约束（方向 D 的研究结论）
- 局限: 需要机器人运动方向信息（可从全景帧的全局光流方向估计）

#### 策略 3: 光流 + 速度联合代理（最佳但稍复杂）

```
兼顾两者:
- 在球面上生成一组合成视口候选（运动前方 ± 一定角度）
- 每个候选的权重由其平均光流决定（光流越小权重越高）
- 选权重最大的候选作为视口中心

center = argmax_candidates [ 1 / (flow_mean(candidate) + ε) × angular_penalty(candidate, forward) ]
```

- 优点: 同时利用"人看运动前方"和"人看稳定区域"两个先验
- 推荐在最终版本中使用

**输出**: 45 维向量 `w_view[45]`，值域 [0, 1]，值越大表示视口在该 tile 的概率越高

---

### 2.3 带宽估计模块（秒级更新）

**目标**: 实时估计当前 5G 上行有效带宽，作为全局 QP 基准调整的依据。

**方法**: 推流缓冲区反馈估计

```
B_est[t] = 当前推流发送缓冲区占用率估计的有效带宽

具体实现（FFmpeg RTMP 推流）:
  - 监控 RTMP 发送缓冲区的排空速率
  - 或通过 SRTP SRT 的 ACK 回传 RTT 间接估计
  - 或简单方案：监控推流端输出码率 vs 发送速率，差值 = 拥塞信号

简化方案 (EWMA 平滑):
  B_est[n] = β × B_measured[n] + (1-β) × B_est[n-1]
  β = 0.3 (典型值，平滑但不延迟)
```

**5G 上行带宽波动特征**:

| 条件 | 典型上行带宽 | 波动特征 |
|---|---|---|
| 好信号、静止 | 50-80 Mbps | 稳定 |
| 中等信号、低速移动 | 20-50 Mbps | 中等波动 |
| 弱信号、高速移动/切换基站 | 5-15 Mbps | 剧烈波动 |

**QP_base 自适应规则**:

```
QP_base(B_est) = 
  clamp(QP_ideal - k_B × (B_est - B_target), QP_min, QP_max)

其中:
  QP_ideal = 23-25 (5Mbps 2880×1440 H.265 的合理起始 QP)
  B_target = 50 Mbps (目标/期望上行带宽)
  k_B = 自适应增益（带宽敏感度）
  QP_min = 18 (最好画质上限)
  QP_max = 38 (最差画质下限，保证基本可辨认)

k_B 设计为不对称:
  - 带宽下降 → QP 快速上升（k_B_high，快速响应拥塞）
  - 带宽恢复 → QP 缓慢下降（k_B_low，避免画质振荡)
```

**QP_base 更新频率**: 每秒 1-2 次，避免过于频繁的画质切换。

---

### 2.4 Tile 划分方案

**推荐方案**: 9×5 = 45 tiles

```
equirectangular 2880×1440:

列 (9列): 每列约 320 像素
┌──┬──┬──┬──┬──┬──┬──┬──┬──┐
│ 0│ 1│ 2│ 3│ 4│ 5│ 6│ 7│ 8│  ← 每行一个 tile
├──┼──┼──┼──┼──┼──┼──┼──┼──┤
│ 9│10│11│12│...              │
├──┼──┼──┼──┼──┼──┼──┼──┼──┤
│                            │
├──┼──┼──┼──┼──┼──┼──┼──┼──┤  ← 第 5 行
└──┴──┴──┴──┴──┴──┴──┴──┴──┘

每 tile: 320×288 像素 (行: 1440/5 = 288, 列: 2880/9 = 320)
```

**为什么 9×5**:
- 水平 9 列：足够细粒度区分"前进方向 ± 40°"内的区域（约 2-3 列 = ~80°视口覆盖）
- 垂直 5 行：全景中地平线附近最重要（中间 3 行），天空和地面（上下行）重要性低
- 45 tiles 的总 QP 偏移表（45 个值）对编码器来说足够轻量

**HEVC x265 tile 编码参数**:
```
FFmpeg 参数:
  -c:v libx265
  -x265-params "tile=9x5:..."
  - 对每个 tile 设置独立 QP 偏移（通过 x265 的 ROI/slice 接口或拼接多流方案）
```

**QP 偏移实现可选方案**:
- **方案 1 (理想)**: x265 内直接支持 per-tile QP offset —— x265 文档显示有限支持
- **方案 2 (重用)**: 将帧切分为 45 块独立帧，分别用不同 QP 编码，再拼回全帧 —— FFmpeg 下完全可行
- **方案 3 (近似)**: 用 x265 的 ROI rate control 参数设定 45 区域的不同 delta QP

---

### 2.5 Tile 优先级融合

**融合公式**:

```
W_tile(i) = α × w_view(i) + β × (1 - w_flow(i)) + γ × w_task(i)
```

| 参数 | 含义 | 典型值/来源 |
|---|---|---|
| α | 视口代理权重 | 0.3-0.4 |
| β | 光流权重（取反后） | 0.3-0.4 |
| γ | 任务语义权重 | 0.2-0.4 |
| w_task(i) | 预定义的巡检重要性图 | 离线标注 |

**w_task 的生成**（离线，一次性）:
- 对园区 3D 地图标注关键设施位置（管道、仪表、阀门、危险品区等）
- 将 3D 位置投影到机器人巡视轨迹各点的全景球面上
- 生成每条轨迹上的"任务重要性热力图"
- 巡检轨迹固定 → w_task 可预计算为查表

**无 w_task 时（初始版本）**:
- 简化：α = 0.5, β = 0.5, γ = 0
- 仅用光流和视口代理

---

### 2.6 QP 映射与编码

**QP 映射函数**:

```
QP_tile(i) = QP_base(B_est) + ΔQP(W_tile(i))

其中:
  ΔQP(W) = -Δ_max + 2·Δ_max × (1 - W)   // 线性映射
         = -Δ_max × (2W - 1)
  
  W ∈ [0, 1] → ΔQP ∈ [+Δ_max, -Δ_max]
  
  高 W (重要 tile)  → ΔQP 为负 → QP 低于基准 → 画质更好
  低 W (不重要 tile) → ΔQP 为正 → QP 高于基准 → 画质更差
```

**Δ_max 的动态调整**:

```
带宽充足 (B_est ≥ B_target):  Δ_max = 2-3   // 已经都很好，差异不大
带宽紧张 (B_est < B_target/2): Δ_max = 6-8   // 差异拉大，好钢用在刀刃上
```

**约束**:
- QP_tile(i) 范围: [QP_min, QP_max] = [18, 38]
- 相邻 tile 的 QP 差 ≤ 6（避免 tile 边界明显的画质跃变，降低"马赛克拼图"感）

---

## 3. QoE 模型

### 3.1 模型结构

```
QoE_total = Σ_t [ Q_viewport(t) + Q_stall(t) + Q_switch(t) + Q_spatial(t) ]
```

### 3.2 各分项定义

#### 3.2.1 视口感知画质 Q_viewport

```
Q_viewport(t) = Σ_i [ W_tile(i) × f(PSNR_tile(i) | flow_i) ]
                  对所有落入视口区域的 tile i 求和

其中:
  W_tile(i) = tile_i 在视口内的面积占比 × w_task(i)
  
  f(PSNR | flow) = PSNR × g(flow)
  g(flow) = 1 / (1 + k × flow_norm)   // 光流衰减函数
  
  k ≈ 0.1-0.3（从 DVA 文献参考值标定）
```

**物理含义**: 光流大的区域，即使客观 PSNR 高，主观"有效感知画质"也低。

#### 3.2.2 卡顿惩罚 Q_stall

```
stall_risk(t) = max(0, 1 - B_est(t) / B_required(当前编码配置))
Q_stall(t) = -α_stall × stall_risk(t)

α_stall ≈ 10-20（卡顿惩罚应与 5-10dB PSNR 下降等效）
```

#### 3.2.3 画质切换惩罚 Q_switch

```
Q_switch(t) = -γ_switch × |Q_viewport(t) - Q_viewport(t-1)|

γ_switch ≈ 1-2（适度的惩罚，避免过度抑制带宽自适应）
```

#### 3.2.4 空间均匀性 Q_spatial

```
Q_spatial(t) = -δ_uniform × std(PSNR_tile(i))
              对所有视口内 tile i 计算

δ_uniform ≈ 0.2-0.5（轻微惩罚，校验 tile 差异化没有过头）
```

### 3.3 参数标定

**不依赖用户实验的三条路径**:

1. **文献引用**: 光流衰减系数 k 从动态视觉敏锐度（DVA）文献获取参考范围
2. **参数敏感性分析**: 在各系数 ±50% 范围内验证方法排名不变 → 证明方法对参数选择稳健
3. **小规模内部验证**: 项目组 5-8 人快速主观对比，确定大致的参数范围

---

## 4. 实现路线图

### 第 1 步：光流计算 + Tile 划分（1-2 天）

**输入**: 一段录制好的巡检全景视频 (2880×1440, H.265 MP4)
**输出**: 每帧的 45 个 tile 光流幅值 `w_flow[45]`

```
工具: Python + OpenCV + NumPy
主要代码:
  - cv2.calcOpticalFlowFarneback()
  - 球面权重修正 (cos(lat))
  - tile 划分与平均
  - 归一化
```

**验证**: 可视化光流热力图覆盖在 equirectangular 帧上，确认合理性

### 第 2 步：视口代理生成（1 天）

**输入**: 第 1 步的光流数据 + 机器人速度信息（可选）
**输出**: 每帧的 45 维视口权重 `w_view[45]`

```
- 策略 1 (光流最小值代理): 直接在 w_flow 基础上生成
- 策略 2 (速度先验, 可选): 需速度日志
- 验证: 可视化视口热力图
```

### 第 3 步：FFmpeg Tile 编码 + QP 偏移（1-2 天）

**输入**: 原始全景视频 + 每帧 QP 偏移表
**输出**: 差异化编码的全景视频码流

```
方案选择 (推荐方案 2/3):
  - 方案 2 (分块编码重拼):
    1. 每帧切分为 45 个 tile 子图像
    2. 每 tile 用独立 QP 值编码 (libx265)
    3. 将编码后的 45 个 tile 拼接回全帧
    → FFmpeg + Python 脚本即可完成
    
  - 方案 3 (ROI rate control):
    使用 x265 的 --roi 参数
    传递 45 区域的 delta QP map
    → 若 x265 对 45 个 ROI 区域支持良好，最便捷
```

### 第 4 步：带宽估计 + QP_base 自适应（1 天）

**输入**: 5G 带宽 trace 或模拟带宽波形
**输出**: QP_base(t) 曲线

```
- 实现 EWMA 带宽估计
- 实现 QP_base(B_est) 映射函数
- 与第 3 步的 tile QP 偏移叠加
```

### 第 5 步：端到端联调 + 对比实验（1-2 天）

**实验设计**:

| 实验条件 | 说明 |
|---|---|
| 测试视频 | 公开驾驶/骑行 360° 视频 × 2-3 条（模拟巡检） |
| 带宽条件 | (a) 50Mbps 稳定, (b) 15Mbps 稳定, (c) 5G 波动 trace 回放 |
| 速度条件 | 低速 (0-1 m/s) / 中速 (1-3 m/s) / 高速 (3+ m/s) 段 |

**对比方案**:

| 方案 | QP | Tile 差异化 | 带宽自适应 |
|---|---|---|---|
| Baseline: 均匀编码 | 固定 | 无 | 无 |
| Scheme 1: 纯光流 | 固定 | 有 | 无 |
| Scheme 2: 光流 + 视口 | 固定 | 有 | 无 |
| **Scheme 3: 光流 + 视口 + 带宽** | **自适应** | **有** | **有** |

**评价指标**:

| 指标 | 含义 |
|---|---|
| 视口加权 PSNR (V-PSNR) | 模拟视口区域的有效画质 |
| 视口加权光流修正 PSNR (FV-PSNR) | 经光流衰减函数修正后的感知画质 |
| QoE 总分 | 含卡顿/切换/均匀性惩罚的综合体验分 |
| 下游检测 mAP (可选) | 预训练检测模型在解码帧上的精度 |
| 码率节省率 (BD-Rate) | 同等画质下节省的码率百分比 |

---

## 5. 测试数据集

### 5.1 公开数据集（初始验证）

| 数据集 | 内容 | 优势 |
|---|---|---|
| VR-Drive / Driving-360 | 开车、骑行 360° 视频 | 运动模式匹配，有速度变化 |
| YouTube 360° driving/riding | 大量摩托车、自行车、车载视频 | 丰富，易获取 |
| 公开 viewport 数据集 | 含部分驾驶类的视口轨迹标注 | 可验证视口预测精度 |

### 5.2 自采巡检数据（最终验证）

- Insta360 X5 录制 2-3 条典型园区巡检轨迹
- 包含低速驻点、中速巡逻、高速转场段
- 标注关键 ROI 区域（可选，用于验证 w_task 增益）

---

## 6. 关键技术参数汇总

| 参数 | 推荐值 | 说明 |
|---|---|---|
| 全景分辨率 | 2880×1440 | Insta360 X5 推流最高 |
| Tile 布局 | 9×5 = 45 | 每 tile 320×288 像素 |
| 光流算法 | Farneback | OpenCV，15-30ms/帧 |
| 视口 FOV | 90°-110° | 典型 VR 头显/全景观看器 FOV |
| QP 范围 | [18, 38] | x265 合理范围 |
| Δ_max (带宽充足) | 2-3 | tile 间 QP 差异小 |
| Δ_max (带宽紧张) | 6-8 | tile 间 QP 差异大 |
| 邻 tile QP 差上限 | ≤ 6 | 防马赛克拼图感 |
| 带宽估计平滑 β | 0.3 | EWMA 系数 |
| 带宽敏感度 k_B | 0.2-0.5 | QP_base 变化速率 |
| 光流衰减 k | 0.1-0.3 | g(flow) = 1/(1+k×flow_norm) |
| α:β:γ (融合权重) | 0.3:0.4:0.3 | 初始值，可调 |
| QP_base 更新频率 | 1-2 Hz | 秒级，避免画质振荡 |

---

## 7. 预期效果

### 7.1 画质增益（关键区域）

- 低速驻点巡检段：关键设备区域（视口 + 任务重要 tile）PSNR 提升 2-4 dB
- 高速转场段：运动前方稳定区域画质维持，侧面高光流区降 3-6 dB（主观不可感知）

### 7.2 带宽适应性

- 带宽充足（50+ Mbps）：全 tile 高画质，差异小
- 带宽紧张（15 Mbps）：自动集中码率到关键 tile，牺牲无关区域
- 带宽波动：QP_base 自适应平滑响应，卡顿发生率显著降低

### 7.3 码率节省

- 同等视口感知画质下，码率节省 20-35%（vs 均匀编码）
- 节省主要来自高速段的高光流区域降质

---

## 8. 后续扩展方向

1. **引入真实视口回传**（长期）: 当 viewer 端支持视口上报后，将代理替换为真实视口 + 代理备份（回传中断时 fallback）
2. **引入时延预算（播放速度）**（中期）: 在播放端增加变速播放能力，进一步扩大系统优化空间
3. **引入任务语义 w_task**（中期）: 离线标注巡检关键区域，补充纯光流+视口无法覆盖的"静态且重要"场景
4. **联合编码-播放优化**（长期）: 将编码参数和播放速度纳入统一优化框架

---

*文档生成日期: 2026-05-05*
